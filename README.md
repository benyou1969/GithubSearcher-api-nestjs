# Github Searcher Nest.js Api

## Setup
install NestJS cli

```shell
npm install -g @nestjs/cli
```

install dependencies
```shell
yarn
```

run the server
```shell
yarn start
```

visit the API
> http://localhost:8000/api